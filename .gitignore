/target
# Local config (contains token) — never committed.
config.toml
# Ignore everything under /data except the workflows symlink pointing at
# ../../project-zun/workflows. v2 has no prompts.toml; user prompts live
# in jobs.db (custom_prompts table) and seed bytes via `zun-admin seed-prompts`.
/data/*
!/data/workflows
.claude
